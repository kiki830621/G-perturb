## 1. Inputs and download-scope

- [x] 1.1 [P] Fetch the companion-repo criterion inputs (`K562_comparison.suppl_table.csv`, `guide_kd_efficiency.suppl_table.csv`) into `analysis/data/raw/` and record the `Th1Th2_validation_summary` gap resolution. Behavior: the cross-cell-type criterion and knockdown covariate are present locally, and the arrayed-criterion gap from #7 is either resolved or documented. Verify: the two CSVs exist under `raw/` and `analysis/data/DATA_AND_ASSUMPTIONS.md` states the `Th1Th2` resolution.
- [x] 1.2 [P] Update `analysis/data/DATA_AND_ASSUMPTIONS.md` §4 so the two per-facet h5mu are the core decomposition inputs and the h5ad-only path is the labelled sanity-check stepping-stone. Behavior: the download-scope decision reflects the reframed deliverable. Verify: content review of §4.

## 2. Design-level decomposition (headline)

- [ ] 2.1 Deliver the **Design-level facet variance decomposition** as the headline (Decision 1: the headline is the design-level G-study; per-target dependability is a shrunk view. Decision 2: distance-based permutational decomposition is primary; Gaussian REML is a baseline comparator). Implement the facet variance shares (guide, donor, condition, target × interactions) via a distance-based / permutational decomposition on the released profiles. Behavior: running it on `DE_stats.h5ad` + `by_guide.h5mu` + `by_donors.h5mu` emits a variance-shares table with one non-negative row per facet, shares plus residual summing to the total dispersion. Verify: the output table is produced and the sum check holds on the downloaded data.
- [ ] 2.2 Deliver the **Irreducible replication floor** from dispersion among identical-facet replicate profiles (guide pair / donor split-half). This is the only quantity labelled "distribution-free" (Decision 6: terminology — distribution-light for the estimator, distribution-free reserved for the floor). Behavior: a floor scalar is reported as an assumption-free lower bound. Verify: the floor appears in the output and is computed with no distributional assumption (code + content review).
- [ ] 2.3 Deliver the **Distribution-light estimator with a Gaussian baseline comparator** (Decision 2: Gaussian REML is a baseline comparator, not the primary). Fit the Gaussian crossed REML decomposition on a scalar summary and report it alongside, distinctly labelled and never presented as the primary result. Behavior: both the distance-based/permutational shares and the Gaussian REML baseline shares appear in the output. Verify: both share sets present and the baseline is labelled distinct from the primary.

## 3. Per-target dependability (shrunk view)

- [ ] 3.1 Deliver the **Donor facet modeled as split-half generalizability** (Decision 4: the donor facet is split-half generalizability, not a one-way effect over four donors). Derive the donor score from the released disjoint donor-pair splits or overlap-aware pair modalities. Behavior: the donor score is derived from split-half agreement. Verify: code + content review confirms no four-level one-way donor variance component.
- [ ] 3.2 Deliver **Per-target shrunk dependability** (Decision 3: Fay-Herriot moment-based empirical Bayes reconciles per-target shrinkage with non-normality). Implement moment-based / Fay-Herriot empirical-Bayes shrinkage to produce per-target `R_dep,t` with intervals, shrinking each per-target statistic toward the pooled design-level value by its measured sampling variance. Behavior: per-target dependability table with intervals; a high-sampling-variance target is shrunk more than a low-variance one. Verify: the shrinkage-monotonicity scenario holds on the output (spot-check two targets).
- [ ] 3.3 As part of **Per-target shrunk dependability**, handle single-guide targets (present in `guide_1` only) as a quality covariate or donor-based fallback, retained and non-zero. Behavior: single-guide targets remain in the per-target output with defined handling. Verify: a known `guide_1`-only target is present and not dropped or zeroed.

## 4. Criterion validation (corroboration)

- [ ] 4.1 Deliver **Criterion validation degrades honestly** (Decision 5: the hackathon cut uses established methods only; the novel estimator is out of scope). Compute reliability→replication against the available criterion data, degrading to `K562`-only and marking the result partial when the arrayed `Th1Th2` table is unavailable, without fabricating the missing leg. Behavior: a criterion-validation statistic with a partial flag when arrayed data is absent. Verify: with the arrayed table absent the output labels the result partial and uses `K562` only.

## 5. Export and demo contract

- [ ] 5.1 Deliver **Outputs conform to the demo export contract**: export the variance-shares table, replication floor, per-target dependability, and criterion statistic as committed artifacts in the #5 demo data-and-figure format. Behavior: the static demo consumes the outputs without a live backend. Verify: the exported files exist in the committed format the demo reads.
- [ ] 5.2 Add an output validation script analogous to `analysis/data/validate_codebook.py` that schema-checks the exported artifacts. Behavior: the script confirms the exported outputs exist and conform to the export schema. Verify: the script exits 0 on valid outputs and non-zero on a missing/malformed artifact.

## 6. Re-weight the method spec

- [x] 6.1 Re-weight `docs/design.md` so the heuristic is the labelled sanity-check stepping-stone, the generalizability decomposition is the deliverable, criterion validation is corroboration, and a **distribution-light / assumption-lean** formulation section is added (Decision 6: terminology — "distribution-light / assumption-lean", with "distribution-free" reserved for the floor). Behavior: the design document reflects the reframed deliverable and the six decisions. Verify: content review confirms the §9 heuristic, §10 hierarchical, and §13 criterion sections are re-weighted and the distribution-light section is present.
- [x] 6.2 [P] Update `README.md` so the stated deliverable is the distribution-light generalizability decomposition rather than the heuristic ranking. Behavior: the README headline deliverable matches the reframe. Verify: content review of the README deliverable statement.
